interface:
  display_name: "Orchestrate Long Task"
  short_description: "Safely continue large tasks across accounts"
  default_prompt: "Use $orchestrate-long-task to inspect this repository and initialize or resume its long-running task with minimal user input."
