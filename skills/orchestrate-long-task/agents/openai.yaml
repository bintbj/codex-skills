interface:
  display_name: "Orchestrate Long Task"
  short_description: "Resume isolated long tasks across accounts"
  default_prompt: "Use $orchestrate-long-task to initialize or resume this mission with Git-backed cross-account isolation."
