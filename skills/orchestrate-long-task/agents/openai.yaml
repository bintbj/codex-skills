interface:
  display_name: "Orchestrate Long Task"
  short_description: "Safely continue large tasks across accounts"
  default_prompt: "Use $orchestrate-long-task to migrate or resume this large task with Git-backed checkpoints."
